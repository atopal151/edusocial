import 'package:flutter/material.dart';

class HotTopicsList extends StatelessWidget {
  const HotTopicsList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}